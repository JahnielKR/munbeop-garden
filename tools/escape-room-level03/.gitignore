# Review-only renders (previews, contact sheets, hotspot overlays, zoom crops).
# Regenerated by running the gen_*.py scripts; not shipped. Mirrors level 1/2,
# whose out/ was likewise kept out of git.
out/

# Python bytecode cache
__pycache__/
*.pyc
