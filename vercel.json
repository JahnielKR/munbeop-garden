{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd munbeop && npm install --legacy-peer-deps --no-package-lock && npm run build && cd .. && rm -rf .vercel/output && cp -r munbeop/.vercel/output .vercel/output",
  "installCommand": "echo 'install handled by buildCommand'"
}
